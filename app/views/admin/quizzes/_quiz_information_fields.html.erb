<div class="nested-fields">
  <%= f.hidden_field :id %>
  <%= f.select :information_id, Information.all.map { |c| ["#{truncate(c.challenge_text, length: 50, separator: ' ')} (#{c.id})", c.id] }, {}, {class: "col-xs-12 col-sm-3" }%>
  <%= link_to_remove_association 'Remove Information', f %>
  <div class="space-2"></div>
  <div class="clearfix"></div>
</div>
