<div class="nested-fields">
      <%= f.hidden_field :id, class: "col-xs-12 col-sm-6"%>
      <%= f.select :supervised_id, User.all.map { |user| ["#{user.name} (#{user.email})", user.legacy_id] }, class: "col-xs-12 col-sm-6"%>
      <%= link_to_remove_association 'remove user', f %>
    <div class="space-2"></div>
</div>
